# MillionFlats (.env / .env.local)
# Copy this file to `.env` (or `.env.local`) and fill in real values.
# IMPORTANT: Never commit real secrets.

# =========================
# REQUIRED (app + database)
# =========================

# PostgreSQL connection string used by Prisma (required for auth + OTP storage)
# Format:
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/millionflats?schema=public"

# JWT signing secret used by /api/auth/* to issue HttpOnly cookies
# Generate a strong random secret (>= 32 chars)
JWT_SECRET="CHANGE_ME__use_a_long_random_secret"

# Base URL of your application (used for Google OAuth redirect URL generation)
# Local:
# http://localhost:3000
# Production:
# https://millionflats.com
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# =========================
# REQUIRED (NextAuth)
# =========================

# NextAuth base URL (MUST match your deployed domain)
NEXTAUTH_URL="http://localhost:3000"

# NextAuth secret (generate a strong random secret)
NEXTAUTH_SECRET="CHANGE_ME__use_a_long_random_secret"

# =================================
# OPTIONAL (only if using Google SSO)
# =================================

# Google OAuth credentials (used by /api/auth/google and /api/auth/google/callback)
# Create OAuth Client in Google Cloud Console
GOOGLE_CLIENT_ID="YOUR_GOOGLE_OAUTH_CLIENT_ID.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_OAUTH_CLIENT_SECRET"

# =========================
# OPTIONAL (platform/runtime)
# =========================

# Next.js uses NODE_ENV internally; set automatically by your runtime.
# NODE_ENV="development"
