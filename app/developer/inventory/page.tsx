import WorkspacePlaceholder from '@/components/dashboard/WorkspacePlaceholder'

export default function DeveloperInventoryPage() {
  return <WorkspacePlaceholder title="Inventory" description="Track availability, stock, unit mix, and project inventory health from one developer workspace." primaryHref="/developer/projects" primaryLabel="View Projects" />
}
