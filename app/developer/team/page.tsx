import WorkspacePlaceholder from '@/components/dashboard/WorkspacePlaceholder'

export default function DeveloperTeamPage() {
  return <WorkspacePlaceholder title="Team" description="Invite teammates, define workspace roles, and manage developer organization access." primaryHref="/developer/settings" primaryLabel="Open Settings" />
}
